PORT=4000
NODE_ENV=production

# En Render monta un Persistent Disk en /var/data y usa:
DATA_DIR=/var/data

# Opcional. Si no los defines, salen de DATA_DIR.
# DB_PATH=/var/data/database.sqlite
# IMAGES_DIR=/var/data/images
# UPLOADS_DIR=/var/data/uploads
